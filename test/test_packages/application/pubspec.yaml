name: application
description: A starting point for Dart libraries or applications.
version: 1.0.0

environment:
  sdk: '>=2.2.0 <3.0.0'

dependencies:
  meta: ^1.0.0
  dependency:
    path: ../dependency/

dev_dependencies:
  pedantic: ^1.0.0
  test: ^1.0.0
