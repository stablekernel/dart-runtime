name: dependency
description: A starting point for Dart libraries or applications.
version: 1.0.0
publish_to: none

environment:
  sdk: '>=2.12.0-0 <3.0.0'

dependencies:
  conduit_runtime:
    path: ../../..

dev_dependencies:
  lint: ^1.5.0
  test: ^1.16.5
